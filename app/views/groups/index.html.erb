<% @groups.each do |group| %>
  <a class='groups' href="/groups/<%= group.id %>">
    <div><img class='avatar-img' src=<%= group.icon %> /></div>
    <div><%= group.name %></div>
    <div><%= group.name %></div>
    <div><%= group.created_at.strftime('%d %b %Y') %></div>
  </a>
<% end %>

<%= link_to('Add New Group', new_group_path) %>