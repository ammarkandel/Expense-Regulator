<h1 class='expenses-title'>Expenses</h1>

<div class='details'>
  <h3>Total amount</h3>
  <h3>$<%= @user_expenses %></h3>
</div>

<%= render partial: 'shared/expense', collection: @expenses %>

<%= link_to 'Add New Transaction', new_expense_path, class: 'btn btn-success' %>
